# One-shot status, for clients that want a single request rather than a subscription.
# A UI should prefer the ~/status topic, which carries this same message latched and on change.

---

RecorderStatus status
